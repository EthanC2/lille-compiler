program foo is
    x : constant integer := 100;

    function bar(x : value integer) return integer is
        y : constant integer := 200;
    begin
        return x + y;
    end;
begin
    writeln( bar(x) );
end;
